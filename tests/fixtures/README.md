# Fixture provenance

Reference outputs target the public Python implementation of
[nagisa 0.3.0](https://github.com/taishi-i/nagisa), created by Taishi Ikeda
and contributors, running under CPython 3.12.

- `ja_parity_subset.jsonl`: 1,717 word-segmentation and POS cases. Inputs include
  synthetic Unicode/whitespace/control probes, dictionary-derived samples,
  and Japanese Wikipedia excerpts. All word/POS outputs were regenerated
  with nagisa 0.3.0 under CPython 3.12.14 on 2026-09-14. The original 1,713
  inputs retain their order; four upstream release regressions were appended.
  Two original cases have corrected words/POS after the upstream BMES fix.
- `prepro_cases.jsonl`: 407 preprocessing/lowercase/character-type cases
  targeting CPython 3.12 / Unicode 15.0.0, independent of model weights.
- `pos_candidates.json`: 6,402 CPython set-order references, covering every
  candidate sequence in the original dictionary plus collisions, duplicate
  IDs, growth and the default noun heuristic. Generated by `tools/pos_reference.py`.
- `pos_tokens.json`: nine pre-segmented POS reference inputs covering empty
  lists, case, normalization, spaces, unknown words and contextual labels.
- `pos_labels.json`: all 24 labels from `nagisa.tagger.postags` in original
  numeric ID order, generated by `tools/pos_reference.py`. Used to validate
  `PosTag` display/parsing and model label IDs.
- `pos_vocabs.pickle`: a synthetic protocol-2 dictionary fixture with shared
  tag-list memo references, used to test the complete vocabulary reader.
- `options.json`: 1,721 lowercase text cases (the corpus above plus four
  synthetic probes), 196 user-dictionary cases, 40 POS selection cases, and
  20 pre-segmented casing cases. Generated by `tools/options_reference.py`.
  Dictionary cases use entries that behave literally in Python; intentional
  Rust differences for regex metacharacters and empty normalized entries
  are covered separately in Rust tests. Wikipedia text retains the source
  attribution recorded for the original corpus entries.
- `compat_030.json`: the 135 upstream stopwords, 12 BMES decoder cases,
  and four release regressions, generated by `tools/compat_reference.py`.
  The regression texts are also appended to the main parity corpus.
- `wikipedia-sources.json`: source article links for all 336 `wiki`, 18
  `wiki_multi`, and one Wikipedia-derived `long` record, indexed by the
  one-based line number in `ja_parity_subset.jsonl`. The other four `long`
  records are synthetic. Sources were recovered from the original corpus's
  cached article extracts; multiple links are retained for duplicate text.

Wikipedia text is credited to the contributors to the linked articles and
is available under [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/),
subject to the [Wikimedia Terms of Use](https://foundation.wikimedia.org/wiki/Policy:Terms_of_Use).
The fixture transformation extracts sentences, strips surrounding whitespace,
and sometimes concatenates consecutive sentences. Article histories linked
from each page identify contributors. These text excerpts retain their
separate license; the crate's Apache-2.0 license does not relicense them.

To regenerate word outputs, run `tools/reference.py` as described in the root
README. To add your own cases, supply JSONL records with `text` and optional
`cat`. Preserve article attribution when adding external corpus text.
